{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",

    // Compila para CommonJS (compatível com api/index.cjs)
    "module": "CommonJS",
    "moduleResolution": "Node",

    // 🔧 Desliga a restrição que está te quebrando:
    "verbatimModuleSyntax": false,

    // Facilita import default de libs CJS (ex.: jsonwebtoken)
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    "target": "ES2022",
    "skipLibCheck": true
  },

  // Não precisamos compilar testes para o deploy
  "exclude": ["src/**/*.spec.ts"]
}
